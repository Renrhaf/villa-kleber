{% extends 'base.html.twig' %}

{% block content %}
    <div class="container-fluid">
        <div class="row slideshow" id="home-slideshow">
            <div class="col-md-12">
                <div class="royalSlider rsDefault">
                    <img class="rsImg" src="{{ asset('img/photos/photo1.jpg') }}" />
                    <img class="rsImg" src="{{ asset('img/photos/photo2.jpg') }}" />
                    <img class="rsImg" src="{{ asset('img/photos/photo3.jpg') }}" />
                </div>
            </div>
        </div>

        <div class="row" id="presentation">
            <div class="col-md-12">
                <div class="section-title">
                    <h2><span>{{ 'title.presentation_letter'|trans }}</span>{{ 'title.presentation'|trans }} <span class="hr"></span></h2>
                </div>

                <div class="container">
                    <div class="row">
                        <div class="col-md-12">
                            <h3>{{ 'presentation.head'|trans }}</h3>
                            <p>
                                {{ 'presentation.text'|trans }}
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        {{ include('partials/room_preview.html.twig') }}
        {{ include('partials/booking.html.twig', {'form': form}) }}
        {{ include('partials/information.html.twig') }}
        {{ include('partials/contact.html.twig') }}
    </div>
{% endblock %}